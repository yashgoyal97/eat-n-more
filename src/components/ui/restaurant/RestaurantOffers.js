import React from "react";

const RestaurantOffers = ({ offer }) => {
  return (
    <div className="mx-5 text-center bg-orange-100 font-semibold py-3 rounded text-red-600">
      ⚠️ WORK IN PROGRESS (RESERVED FOR OFFERS)
    </div>
  );
};

export default RestaurantOffers;
